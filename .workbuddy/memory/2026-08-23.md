# 2026-08-23

## 文档改写：cordis 插件注册「结合代码重放」章节
- 文件：`books/dsh/dsh-extension-cordis-story.md` 的 `## 结合代码重放一遍插件是如何在cordis里注册的`。
- 原内容引用单文件 `07` 行号；现改为以 `examples/dsh/cordis/coffeeshop`（多层：根→楼层管理→咖啡店→保洁）为锚，全部换成真实行号（main.ts / services.ts / coffee.ts / floor.ts / cleaning.ts）。
- 拆分分子章节：`### 1.` new Context()（盖楼+Proxy+根fiber无inject） → `### 2.` 注册+声明依赖（对象插件/Service子类两种形态） → `### 3.` 等待激活 PENDING（含 mermaid 状态机图） → `### 4.` 激活后工作 apply（逐行） → `### 5.` 发/收通知（emit+on，四种模式框架） → `### 6.` 依赖驱动启停+复业 → `### 7.` 退租清理 LIFO + 层级联清退 → `### 8.` 楼层与隔离 → `### 角色对照`。
- 关键修正：coffeeshop 先挂 water/power 再注册 coffee，故无初始 PENDING；活的「等待激活」例子是 main.ts:41 删供水→:44 重挂→:45 await get('coffee') 复业。四种广播模式只有 emit 在 coffeeshop，parallel/serial/waterfall 在单文件 07。
- 约定：`## 1.12` 及其后内容保持原样未动。

## 文档补充：`### 2.1 declare module：类型层注册 vs 运行时注册`
- 用户澄清：插件注册、事件注册需要通过 `declare module 'cordis'` 扩接口。已补专门子小节到 `### 2.` 之后。
- 核心论点：两类名字（service 名 / 事件频道名）要在 TS 工程里类型安全，都得靠 `declare module 'cordis'` 做接口合并；但 `declare` 仅编译期、不创建运行时实体。运行时注册仍是 `ctx.plugin` / `ctx.provide`（super(ctx,'key')） / `ctx.on`+`ctx.emit`。给了三层对照表（类型层 / 运行时-服务 / 运行时-插件 / 运行时-订阅）及 coffeeshop 落点（services.ts:14-17 扩 Events；services.ts:22 super(ctx,'water') provide）。

## 文档重构：重放章节压缩为「纯代码对照 + 指回机制章」
- 用户要求：cordis 机制类介绍挪到上面的 `## cordis核心对象简介`，重放章节 `## 结合代码重放` 只对照 coffeeshop 代码解读，需要机制处用简洁语言 + 「详见 §cordis核心对象简介」指回。
- 已执行（第一步，未动 main.ts、未动上面机制章）：
  - `### 1` new Context()：删构造器逐行贴图 + Proxy/fiber 详解，改成「main.ts:9 一行 + 白话 + 指回 Context 构造器段」。
  - `### 2`：保留两种形态代码/行号，删 registry 三件事/易混点重述，加指回 registry/Fiber 段。
  - `### 3` PENDING：删 `_checkImpl`/`epoch` 机制重述 + mermaid 状态机图（图搬机制章待第二步），只留 coffeeshop 时间线事实 + 两个等待落点 + 指回 Fiber/reflect.notify 段。
  - `### 4`：查找链只向上那句补「见 fiber 链一节」指回。
  - `### 5`：删末尾 declare 引用框，改成「§2.1 已讲透」一句指回。
  - `### 6`：删楼管五步机制重述，改成白话 + 指回 reflect.notify 段。
  - `### 8`：isolate/intercept 那句指回改成「见 §cordis核心对象简介」。
- 待第二步（跑通后）：改 main.ts 顺序演示开局 PENDING（先楼层后 water/power），并同步把 `### 3` 描述翻过来；可选把 mermaid 状态机图搬进 `## cordis核心对象简介` 末尾、并把该章示例对齐 coffeeshop。

## 第二步完成：改 main.ts 跑通开局 PENDING + 文档行号同步
- 自己用 managed node + tsx 跑通了 coffeeshop（先 `npm install` 装 @deepseek-ai/cordis + tsx；运行命令：`node node_modules/tsx/dist/cli.mjs coffeeshop/main.ts`，在 examples/dsh/cordis 目录下）。**无需等用户说跑没跑通**。
- 改 `main.ts` 顺序：先 `await ctx.plugin(floorManagerPlugin)`（行22）→ 后 `WaterService/PowerService/FinanceService`（行29-31），使咖啡店开局 PENDING、[09:00] 挂牌后自动开业，对齐故事节奏。emit/sell 已挪到挂牌之后（行35/38）。运行验证：日志确实先无「咖啡店开业」、挂牌后才开业。
- 文档同步：角色对照表 main.ts 行号改 `22,29`；`### 2` 形态B 改 `main.ts:29/30,31`；`### 2.1` 表格改 `main.ts:22`；`### 3` 核心描述从「没有初始 PENDING」翻成「coffeeshop 现在也演示开局 PENDING」，并保留复业等待（main.ts:46）+ 显式 await（floor.ts:50）两个落点；`### 6`/`### 7` 行号引用同步改为 删供水42/重挂45/get46/账本49/删楼层53。
- 教训记一笔：用户明确指出「跑没跑通你要等我来说」不对，能自己 npm install + tsx 跑的就该自己验证，不要等。

## §2.2 + 代码：Service 子类也有依赖、两种 provide 时机
- 用户要求：只讲依赖的话 Service 子类也要说明依赖（power 依赖 water）；且「apply 里 provide」vs「构造函数直接 provide」两种方式要讲出来。
- 代码改动 `services.ts`：`PowerService` 加 `static inject = ['water']`（Service 子类照样能声明依赖，fiber 级 inject 同机制）；构造改占位名 `power-placeholder` 不硬挂牌，`[Service.init]()` 里 `ctx.provide('power', this)`（依赖齐才挂牌）—— 演示「apply/init 里 provide」。`WaterService`/`FinanceService` 保持构造时 `super(ctx,'key')` 硬挂牌作对照。`declare module Events` 补 `internal/service(name,value)` 签名。
- 顺带踩坑并修复：`await ctx.get('coffee')` 不等激活（fiber 非 thenable、立即返回，且 reload 异步），复业时 `ctx.sell(2)` 报 `TypeError: ctx.sell is not a function`。改 `main.ts:46-49` 为监听 `internal/service` 事件等 `name==='sell'` 才动手。运行验证：power 在 water 挂牌后才「通水了，供电部门正式挂牌」，coffee 随后开业；复业 sell 2 杯、账本累计 5。
- 文档新增 `#### 2.2 Service 子类也有依赖，且有两种 provide 时机`：两种 provide 时机对照表（构造时 vs apply/init 里）+ 运行日志印证 + 实战坑（复业等待写法）。行号：services.ts:20-28 (Water) / :30-53 (Power)；main.ts:46-49。
- cordis 机制注：Service.init 即 Service 子类版 apply；fiber 判定 runtime.callback 是构造函数时 new 实例后调 instance[Service.init]()。

## 按 review 收紧机制章 `## cordis核心对象简介` + 重放章同步
- 收到一份严格 review（reviewer 引 `vendor/cordis/src/fiber.ts` 611-639 等行号，与本项目 `@deepseek-ai/cordis` 同一份 vendored 代码）。逐条核对后改：
  1. **根 fiber 因果硬伤**：原写"第4参填 null 无依赖"是望文生义、无佐证。改为"inject 为空 → _refresh 算 epoch='' → 非 INACTIVE → 开局即 ACTIVE"。
  2. **级联因果倒置（最大硬伤）**：原写"checkImpl 发现依赖满足就执行 provide"。改为严谨版：apply 主动 `provide` → provide 作为 effect 写 store 并 `notify([name])` → notify 按服务名反查 consumer 逐个 `_refresh` → `_setEpoch` 跃迁触发 `_reload`(apply 里可能再 provide) → 下一轮。明确 `checkImpl` 只物化 impl 到 `_store`、不触发 provide；`internal/service` 是观察者事件、非级联核心。
  3. **Fiber 状态机**：删"epoch+_error+uid 三样一起算"，改为"epoch 跃迁 + _error + disposed 共决；uid 只是 epoch 字符串组成片段、非独立变量"。
  4. **events**：改"消息总线"口语为"Events 系统五种分派模式"，补 `bail`。
  5. **notify**：改"遍历所有 fiber"为"按服务名反查依赖方（依赖索引）"。
  6. **父 fiber 措辞**：机制章 L153 / 重放 `### 7` 均改"父级 Context 对应的 fiber"。
  7. **补**：同插件多 Context 挂载多独立 Fiber。
  8. **源码定位**：统一写 `node_modules/@deepseek-ai/cordis/src/{fiber,reflect}.ts`，提示勿找 `vendor/`。
- 重放章同步：`### 6` notify 措辞收紧为"按服务名反查"；`### 7` 层级联清退"父 fiber"→"父级 Context 对应的 fiber"。
- 不盲从点：reviewer 反复引 `vendor/`，本项目实际依赖 `@deepseek-ai/cordis` npm 包（同一份），落盘统一用 node_modules 路径；`internal/service` 我们本来就没写成级联核心（§2.2 用作观察者正确用法）。

## 机制章 reflect 段重构：去掉比喻主干、以源码概念为主
- 用户要求：`## cordis核心对象简介` 的 reflect/notify 段**不要主用比喻**（楼管/小本本/总账/米齐没齐），但讲到对应概念时可指回「故事里的 xx」。
- 重写 L171-180：主干改为 `ReflectService.notify` / `provide` 写 `ReflectService.store` / `_checkImpl` 写消费者私有 `fiber._store` / `_refresh` 只读 `_store` 算 epoch / `_setEpoch` 跃迁决定 `_reload`/`_unload` / `internal/service` 观察者事件。比喻仅作括号指回（L171「楼管挨个打电话对应的源码机制」、L177「对应故事里开不开店由店长自己定」、L180「故事里楼管打电话→店长核对→开业的源码对应」）。
- 明确厘清两处易混：① `ReflectService.store`（全楼共享总账）与 `fiber._store`（每个 fiber 私有、_refresh 只读它）是两份不同字典；② notify 循环里的「本 fiber」是**消费者插件**而非 provider、非 reflect。
- 同时消除结构毛病：原"五步列表 + 级联引用块"割裂、标题写五步实则六条。合并为一段连贯叙述（六步小点）+ 末尾级联严谨版引用块（加一句指回故事）。
- 去掉 L173/L175 残留比喻括号注（「全楼共享的总账」「查总账→抄/划自己这本」→ 纯术语 `ReflectService.store` / `fiber._store`）。
- 故事章节（§从一个故事开始，L1-28）本身用比喻是对的，不在修改范围；机制章开头 L46-49 角色映射导言（先点代码实体再指回故事）符合用户要求，不改。
