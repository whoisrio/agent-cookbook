# 2026-08-25

## books/dsh/dsh-cordis-code-example.md：孔太斯咖啡店样例介绍文档
- 用户要求把 coffeeshop 样例写成介绍文档（含内容/依赖关系/各 demo 输出/关键代码），写到 `books/dsh/dsh-cordis-code-example.md`（原文件为空，直接重写）。
- 结构：①文件清单表 ②插件依赖关系（ASCII 依赖图 + 时间线表，强调 `PowerService.inject=['water']`）③公告牌机制（贴 NoticeBoard/trackEffect/trackEvent/installLifecycleBoard 关键代码）④关键插件代码（services/floor/coffee/fridge）⑤三个 demo 输出（跑 `tsx` 抓真实输出后节选粘贴）⑥派发语义对照表 + 小结。
- 所有输出均来自实跑（`npx tsx coffeeshop/0x-*.ts` 落盘后读），关键代码逐字取自磁盘文件，未编造。临时输出文件已清理。
- 落点惯例：样例讲解文档放 `books/dsh/`，可跑代码在 `examples/dsh/cordis/coffeeshop/`。

## 文档压缩为「视频精讲版」
- 用户反馈原文档内容偏多，要求压到支撑 5 分钟视频。已重写 `books/dsh/dsh-cordis-code-example.md`：砍掉大段公告牌输出粘贴、重复代码与文件清单细节，保留三核心机制、依赖关系图、NoticeBoard 生命周期看板关键代码、coffee trackEffect 示例、各 demo 抓数字/转折点（21条/21→2/21→18）、四种派发语义对照表、一句话收尾。

## 三篇文档链路对齐（story → core-mech → code-example）
- 用户指出 `dsh-cordis-code-example.md` 非独立文档，表达要衔接 `dsh-cordis-core-mech.md`；而 core-mech 又衔接 `dsh-cordis-story-only.md`（故事隐喻：大楼=Context、招商办=registry、楼管=reflect、店长=fiber、秘书处=logger、广播=event）。
- 原 story-only 没讲公告牌。已在其末尾补 `### **公告牌。**` 一节：用「大堂实时公告牌」隐喻表达 provide→在册、dispose→撕掉、effect/订阅同理，并回指 code-example 的 `NoticeBoard`/`trackEffect`/`trackEvent`。
- 同步把 code-example 开头改为「故事里大楼大堂立的那块实时公告牌，在这份样例里落地成一张 ctx 持有的真实对象 NoticeBoard」，闭合 story→code-example 链路。

## Code Example 重写为「视频讲解稿」+ 实现收敛
- 用户指出原文档像在回答他的问题、不像面向视频观众；要求结构改为：①开场承接 core-mech（"用真实样例带大家过一遍故事里的内容"）+ 角色↔代码对照表 + 说明新增 AC/Fridge 两个仅依赖 power 的商家用于模拟 waterfall 停电/涨价通知；②公告牌机制与实现（面向观众讲清「被谁持有=根 Context 全局资源、机制=三类条目怎么登记、回应"为什么不是插件监听消息呈现 effect"）；③三个 demo 逐一附真实运行输出。
- 实现收敛：把 `trackEffect`/`trackEvent` 的 `board.add` 从 effect 外部移进 `ctx.effect` 回调内（建立时 add、dispose 时 remove），让登记真正发生在 effect 生命周期里。重跑三个 demo 确认输出不变（21条/21→2/21→18/账本12）。
- 文档重写 `books/dsh/dsh-cordis-code-example.md`：开场角色对照表、公告牌三段（全局资源/lifecycle 内登记/internal/service 同步 + 三点回应疑问）、演示1 全量输出、演示2 停水前后 board+复业账本12、演示3 四条消息逐一+before/after board。临时输出文件已清理。

## 公告牌简化：去掉「服务条目 / internal/service 同步」
- 用户质疑 2.3「服务条目靠 internal/service 事件同步」过度设计：board 应只演示「插件对全局资源的影响」——插件加载时往公共 board 加一条、unload 时移除；服务 provide 是框架 Service 内部语义，不该塞进 board（否则要监听 internal/service、排除 board 自身、区分 provide/unprovide，复杂且偏离主题）。
- 代码改动 `examples/dsh/cordis/coffeeshop/common.ts`：删 `internal/service` 监听段；`installLifecycleBoard` 改名 `installLifecycleLog`（只打印 ✅/⬇️ 开业停业标题，不再碰 board）；删 `NoticeBoard.removeOwner`；`BoardKind` 去掉 `'provide'`。三个 demo 的 import+调用改名；`coffeeshop/README.md` 同步：函数名 + 过时 `getEffects()` 看板描述改为 `trackEffect`/`trackEvent` 真相。
- 重跑三 demo 确认：board 只剩 effect(6)+event(9) 两类，演示1 全楼 **15 条**、演示2 停水 **0 条（空）**、演示3 卸载 fridge **15→12 条**（少 fridge 3 条）；账本跨停业保留 12。
- 文档 `dsh-cordis-code-example.md` 删 2.3「服务条目」整节，公告牌只讲「全局资源 + effect 生命周期内 add/remove」，三处演示输出同步为新真实结果。
