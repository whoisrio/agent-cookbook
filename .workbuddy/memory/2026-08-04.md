# 2026-08-04

## 收口 middleware 为权威专章（第 10 章）

- 用户提出"很有必要出一篇单独讲 middleware 能力的文档"。先反驳/核实：仓库里已有 `10-langchain-middleware.md`，但只是 25 行废片段（从 `create_agent-analysis.md` 抠出的 §4.2）；middleware 在 09 / 源码剖析 / deepagent 里都讲过，概念本身是 `langchain.agents.middleware`（langchain 1.3.14 真实模块，官方文档 docs.langchain.com/oss/python/langchain/middleware）。
- 用户确认定位：收口成权威专章，09 与源码剖析改为引用 10。
- 行动：
  - 重写 `10-langchain-middleware.md` 为完整章节（六 hook + 生命周期图、装饰器写法、jump_to/can_jump_to、多 middleware 串并联顺序、内置库清单、子图嵌入、节点全景图、速查表、指路）。
  - API 已对照真实源码核实：`langchain.agents.middleware` 导出 `before_agent/before_model/after_model/after_agent/wrap_model_call/wrap_tool_call` + 大量内置 middleware（Summarization/HITL/ToolRetry/ModelFallback/ModelCallLimit/PII/ShellTool/TodoList/ToolError 等）。
  - 09：`## 四` 误挂的"create_agent 源码剖析"标题改为 `## 三、middleware：...`，原 `## 三` 改为 `## 四`（修复三/四序号错挂）；删除重复的 6-hook 文字列表与 State/Input/Output Schema 段，改为指路 10。
  - `create_agent-analysis.md`：§4.3-4.6 与 §7.7 关键细节/§7.8 连接顺序改为指路 10，保留源码剖析独有的实现洞察（is not 检测重写、wrap_ 双实现严格性）。
  - README 目录已加第 10 章；notebook 表加 `src/baby_agent/agent.py` 作为 ch10 可跑示例。
- 未做：未新建 `examples/10-middleware.ipynb`（ch10 可跑示例暂用 baby_agent.py）。若要对齐 notebook 约定可后续补。
