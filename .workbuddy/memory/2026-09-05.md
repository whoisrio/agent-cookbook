# 2026-09-05

## 新系列启动: books/python/ (异步编程 + 事件驱动 Agent Loop)
- 用户要写两篇: ①Python 异步编程 ②生产级事件驱动 Agent Loop 设计。已讨论定稿两篇大纲。
- 反驳/纠正过的概念点（用户认可后写进文档）:
  - GIL 不是协程存在的理由（Node.js 反例），GIL 只解释多线程不能并行算;
  - Task 是 asyncio 家当, concurrent.futures 只有 Executor+Future, threading 无 Future;
  - 两个 Future（asyncio vs concurrent.futures）同名不同类不混用;
  - "让出 CPU 去处理非 I/O 耗时逻辑"说法不对——CPU 密集任务在协程单线程会卡死 loop。
- 第二篇核心论点（已与用户对齐）: 紧急消息插入同步架构可用轮询硬扛, 不是最强理由; 最强的是"阻塞时连轮询执行权都没有"（打断/取消）和"挂起等外部事件"（HITL 审批等三天）+ 会话隔离约束（同会话串行/跨会话并行）。三档场景框架用户认可。
- 第二篇案例已核实: DeepSeek Harness(dsh, 底层 Cordis——用户 books/dsh 已有拆解)、OpenClaw(龙虾, 2026 初 34 万 star)、Hermes(Nous Research, adapter→event bus→agent loop, agent:step 事件)、Pi。
- 交付: `books/python/01-python-async.md`(7 章正文) + `examples/python/async-basics.ipynb`(19 cell, Python 3.13.12 全部实测, 输出为真实运行结果)。贯穿场景: A/B/C 三用户剧本, 同步 6s → 协程 2s。
- **注意**: `examples/python/async-demo.ipynb` 是用户自己起的草稿（4 cell, 含 GIL 表述和 ProcessPoolExecutor 报错 cell）, 本次未动它, 新样例另建了 async-basics.ipynb。后续是否合并由用户定。
- 第二篇 `books/python/02-event-driven-agent-loop.md` 待写, 大纲已定（场景剧本+事件驱动版对比、案例源码级拆解待用户定深度）。

## 第一篇返工（用户反馈: 太浅 + 开场别用 LLM 举例）
- 用户批评 v1: ①每个对象没交代来自哪个包 ②不需要上来就拿 LLM 调用举例。已重写 v2。
- v2 关键改动: 开场改通用 I/O 场景（抓三个网页, Agent 场景只在开头结尾轻提）; 新增"先给地图"章（四层: 语言层 types.CoroutineType/collections.abc/PEP 342 → asyncio 包按源码文件列模块 → selectors 包 → concurrent.futures+threading）; Event Loop 深拆（loop 实测类型 _UnixSelectorEventLoop/MRO、_selector=KqueueSelector、_ready deque/_scheduled list+heapq、_run_once 骨架）; await 机制源码级（futures.py L286 `yield self`; tasks.py L304 `coro.send(None)`/L306 `coro.throw(exc)`/L340-343 `add_done_callback(__wakeup)`+`_fut_waiter`）; 挂起-登记-恢复六步链。
- 事实核实方法: pkgutil 列 asyncio 包内模块 + inspect.getsourcefile 定位定义文件 + 内省 loop._selector/_ready/_scheduled + 读 CPython 3.13 源码行号。CPython 源码在 /opt/miniconda3/lib/python3.13/asyncio/。
- 坑: f-string 里计时表达式先于 await 求值会打出错误时刻（"0.0s"假象）, 计时必须先 await 结果再取 perf_counter——已写进文档提醒。
- asyncio.Future/Task 运行时 `__module__ == '_asyncio'`（C 加速）, 纯 Python 实现在 futures.py/tasks.py（Task L71 `class Task(futures._PyFuture)`）。

## 第一篇 v3（用户再次反馈: 开头地图+罗列包出处是 AI 味, 给了新骨架）
- 用户明确反感"先给地图、罗列每个角色来自哪个包"的写法——**对象出处不要开专章罗列, 要融进叙述**。v2 的地图章已删。
- 用户给的骨架（v3 已照做）: ①Python 异步的三个标准库（开场）②什么是协程 ③Event Loop 与 await 机制 ④中断与 Future/Task ⑤多线程包装 I/O（末尾稍提多进程）。开场用基础例子讲 I/O/CPU（精简）, LLM 调用本质就是网络请求 I/O, 不用特意举例。
- 口径确认: "三个包"实际是 asyncio + concurrent.futures 两个包 + threading/_thread 两个模块, concurrent 本身是空壳——已写进文档。
- v3 新增实测: CPU 密集协程卡死 loop 的心跳演示（0.5~2.0s 四次心跳全部消失）; 多进程 ProcessPoolExecutor 4 任务 4.0s→1.4s; 多进程脚本必须 `if __name__=="__main__"` 保护 + 不能在 jupyter 跑（notebook `__main__` 非文件, spawn 子进程 import 不到, 实测 BrokenProcessPool）——notebook 因此不放多进程 cell。
- 深度保留但融入叙述: 源码行号（futures.py L286、tasks.py L304/306/340-343）放进 await 机制章; 双 Future/Task 归属放进多线程章; 用户口径表述（"剩余逻辑挂起等待、I/O 结束被唤醒"）保留并精确化。
