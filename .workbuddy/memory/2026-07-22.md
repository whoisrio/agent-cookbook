# 2026-07-22 工作记录

## 02 章补齐三种 Workflow 模式示例
用户要求：把第 1 章列的 6 种 workflow pattern 中、02 章还没给代码的 Router / 嵌套子图 / Agent 三种补上，节点能共用的共用，每段代码前补说明。

实现要点：
- 三种模式全部复用前文已定义的 `genJoke` / `translate` / `reviewJoke` / `checkReviewResult`，不重贴。Router 新增 `genKnockKnock` + 路由节点；嵌套子图把 Generator-Evaluator 流水线 `compile()` 后直接 `add_node` 进父图；Agent 用「决策节点→条件边→执行节点→回决策」的循环拓扑，上限用 `iterations` 计数兜底。
- 官方文档已核实两处关键 API：①父图与子图**共享 state key** 时，可直接把编译后的子图传给 `add_node`（无需包装函数）；②Agent 标准拓扑即 conditional-edge 回到自身节点、无工具调用则 END。
- 验证：用打桩 `model`/`get_config` 写 `/tmp/verify_patterns.py`，把三段图真实 `compile()` 并各 `invoke` 一次，全部通过（Router 正确分发、子图当节点正常、Agent 循环跑到 finish 后 `iterations=4` 正常终止）。已删临时脚本。**结论：文档里 LangGraph API 用法无误，可发出。**

## 维护须知
- 02 章现有代码块均假设 `from src.agent_cookbook import model, config`，`model` 是 instructor 封装的结构化输出客户端（`response_model=` 写法）。新增示例延续此风格，未引入 langchain ChatOpenAI。
- 验证 LangGraph 图代码的可复用手法：打桩 model/get_config 后 `compile()` + `invoke()`（不耗真 API key 即可抓出 `add_conditional_edges` / END 作路由目标 / 子图当节点 等误用）。

## 更正：示例落点应为 notebook（非 .md）
- 上面对"三种模式"的理解有误：用户要的是补进 `examples/02-langgraph-basics.ipynb`，不是 `chapters/langgraph/02-langgraph-basics.md`。实际落地：在 notebook 补齐了 **4 种** pattern（orchestrator-worker + router + 嵌套子图 + agent），连同原有的 simpleGraph/promptChain/promptChainWithReview，02 notebook 现含 6 种 workflow pattern 完整可跑示例，节点共用、每段代码前配 markdown 说明 + mermaid。
- `.md` 里上轮多加的"#### 补齐其余几种 Workflow 模式（共用节点）"整节（router/嵌套子图/agent 代码）已删除撤回（删 7431 字符）。`.md` 现在只留 prompt chain / generator-evaluator / orchestrator-worker 三个示意片段 + 可视化小节。
- **约定（用户明确要求）**：可跑的示例代码落在 `examples/*.ipynb` notebook；`chapters/` 的 .md 教程正文只放示意性片段与概念讲解，不堆完整代码。之后补示例优先改 notebook。

## 二次更正：workflow 模式示例归 01 notebook（非 02）
- 上面对「示例落点应为 notebook」的更正仍错：6 种 workflow pattern 的完整示例应补进 `examples/01-workflow-patterns.ipynb`，不是 `examples/02-langgraph-basics.ipynb`（02 是 LangGraph 基础，只讲 StateGraph/Node/Edge 用法）。
- 已重写 01 notebook：18 cells，公共头部 + 共享节点（genJoke/translate/reviewJoke/checkReviewResult）+ 6 种模式完整示例（promptChain / generator-evaluator / orchestrator-worker / router / 嵌套子图 / agent），节点共用、每段代码前配 markdown 说明 + mermaid；原 01 notebook 仅 PromptChain + Orchestrator-Worker 两节且 orc 段有拼写 bug（completed_sections 混写），已一并修干净。
- 02 notebook 撤回上一轮误加的 4 段（8 cells），回到 10 cells（simpleGraph / promptChain / promptChainWithReview / 小结）。
- 验证：01 notebook 6 段代码用打桩 model 实跑 compile + invoke 全过（含嵌套子图当节点、agent 循环到 finish 终止、router 分发、generator-evaluator 评审通过）。

## 二次更正：workflow 模式示例归 01 notebook（非 02）
- 上条「更正」仍错：用户最终明确 6 种 workflow pattern 的完整可跑示例应补进 **`examples/01-workflow-patterns.ipynb`**，不是 02 notebook（02 是 LangGraph 基础：StateGraph/Node/Edge/条件边，只放 simpleGraph / promptChain / promptChainWithReview 三节 + 小结）。
- 已重写 01 notebook：18 cells，公共头部 + 共享节点（genJoke/translate/reviewJoke/checkReviewResult）+ 6 种模式完整示例（promptChain / generator-evaluator / orchestrator-worker / router / 嵌套子图 / agent），节点共用、每段代码前配 markdown 说明 + mermaid。原 01 notebook 仅 PromptChain + Orchestrator-Worker 两节且 orc 段有拼写 bug（completed_sections 混写），已一并修干净。
- 02 notebook 撤回上一轮误加的 4 段（8 cells），回到 10 cells（simpleGraph / promptChain / promptChainWithReview / 小结）。
- 验证：01 notebook 6 段用打桩 model 实跑 compile + invoke 全过（含嵌套子图当节点、agent 循环到 finish 终止、router 分发、generator-evaluator 评审通过）。
