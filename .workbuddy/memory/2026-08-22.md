# 2026-08-22 工作日志

## 处理 agent-cookbook 的 main / origin/main 分叉

- 现象:本地 `ab9efe4`「add dsh and pi」与远程 `734510e`(提交信息只写"update create-agent",实际还做了 books→chapters 重组、加了 result.txt、改了 06 章)各领先 1,分叉。
- 关键坑:本地 `ab9efe4` 在加 dsh/pi 时**把目录结构从 chapters/ 整个退回成了 books/**(langgraph/deepagent 也被挪回 books/)。基线 7f0c6d5 本就带 chapters/。所以这是**结构冲突**,git 重命名探测会把它塌回 books/,普通 merge/rebase 都会丢掉远程的 chapters/ 重组和 result.txt。
- 第一次尝试 `git rebase origin/main` 是**假成功**:44afad8 树与 ab9efe4 一致(books/-only),远程内容没接进来。已 `git reset --hard ab9efe4` 撤销。
- 最终接法(用户拍板):`git reset --hard origin/main` 拿干净 chapters/ 结构,再从 ab9efe4 `git checkout` 回 books/dsh、books/pi、examples/dsh、examples/pi 四个目录,提交为 `7e3b0c5`「add dsh and pi」。
- 结论布局(混合,用户本意):langgraph/deepagent 在 `chapters/`,dsh/pi 在 `books/`(和 `examples/dsh`、`examples/pi`)。远程的 result.txt 与 06 章修改保留。
- 已于 2026-08-22 10:08 推到 origin/main(`734510e..7e3b0c5`),本地与远程已同步。
- 注:`.workbuddy/memory/2026-08-22.md` 本次作为未跟踪文件留在本地(WorkBuddy 项目记忆,未纳入提交)。
