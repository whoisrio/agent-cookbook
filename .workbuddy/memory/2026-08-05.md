# 2026-08-05 工作日志

## 09-agent-loop.md 复查 + 补默认内置 middleware 章节
- 用户要求：补充 09 第 235 行「langchain默认提供了哪些开箱即用的middleware」空章节，并严格 review。
- 发现仓库已被用户大改（非我昨日状态）：`10-langchain-middleware.md` 改名成 `05-langchain-middleware.md`；09 拆成主文 `09-agent-loop.md` + `09-agent-loop-appendix.md`。
- 核实内置类名：用 `.venv` 跑 `langchain.agents.middleware.__all__`，确认 05 第六节清单名字基本都对；额外发现 05 漏列 `FilesystemFileSearchMiddleware`、`ProviderToolSearchMiddleware`（已写入 MEMORY 已知问题）。
- 实际改动（仅 09，未动文件名）：
  1. 补全空的「默认内置 middleware」章节：分类清单（工具选择/重试降级/护栏/会话记忆）+ 可跑示例 + 指针指《实战：Middleware》第六节（teaser，不抄 05 全文）。
  2. 修第 157 行语法病句（"这套生命周期 hook 来实现,六个插入点" → 通顺句）。
  3. 修第 231 行事实错误（"每个 hook 都成独立 node" → 明确 wrap_* 不成 node、是 model/tools 内部拦截链，与 05/附录一致）。
  4. 修两处拼写 `middelware`→`middleware`（第 113、162 行）。
- 结构性问题（用户拍板，未擅自改）：两个 05 撞号；README 死链（指向已删 10、未列 appendix）；09 主文与 05 内容重叠（用户当前设计有意）。

## middleware 模块导出名核实（用户质疑"列出的不全是直接可用的"）
- 用 `.venv` 跑 `langchain.agents.middleware` 全量核实（langchain 1.3.14）：`__all__` 共 42 个名字，但只有 **16 个**是 `AgentMiddleware` 子类（可直接丢 `middleware=[...]`）。
- 其余 26 个**非 middleware**：执行策略（Docker/Host/CodexExecutionPolicy，给 ShellToolMiddleware 当参数）、状态 schema（AgentState/Input/Output）、辅助类型（Runtime/InterruptOnConfig/ModelRequest/Response/RedactionRule/TriggerClause/PIIDetectionError 等）、装饰器工厂函数（before_model/after_model/before_agent/after_agent/wrap_tool_call/dynamic_prompt/hook_config——写自定义 middleware 用）。
- 16 个真 middleware 里还有 **5 个需必填构造参数**、不算免配置直用：FilesystemFileSearchMiddleware(root_path)、HumanInTheLoopMiddleware(interrupt_on)、ModelFallbackMiddleware(first_model+additional_models)、PIIMiddleware(pii_type)、SummarizationMiddleware(model)。其中我文档清单里的 PII、HITL 就属此类。
- 免配置直用 11 个：ContextEditingMiddleware、LLMToolEmulator、LLMToolSelectorMiddleware、ModelCallLimitMiddleware、ModelRetryMiddleware、ProviderToolSearchMiddleware、ShellToolMiddleware(一般传 execution_policy)、TodoListMiddleware、ToolCallLimitMiddleware、ToolErrorMiddleware、ToolRetryMiddleware。
- **文档缺陷**：09/05 的内置库清单平铺、没标"需配置"、也没说明 26 个非 middleware 导出。用户已点出。待办：把清单拆"免配置/需配置"两组并补一句非 middleware 导出说明（用户尚未拍板是否改）。
