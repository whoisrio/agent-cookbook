## stage02 完成（rio 指令：把 stage2 做完）
- 交付：src/baby_event_driven_agent/stages/stage02_inbox_steering/（events/llm/
  agent/__main__/testing + tests 4 passed）+ books/event-driven-agent/
  02-inbox-steering.md + pyproject 与 .venv/bin 的 stage02-demo/stage02-test。
- 设计定型（与 09-08 讨论一致但更简）：每 session 一个 asyncio.Queue 收件箱 +
  常驻 worker（首次消息 spawn，永不自行退出，stop() 显式取消）——"消息进队但
  task 死了"竞态在设计层面不存在；消费端分类：step 边界 drain=steering（拼进
  当前上下文），worker 空闲取到=followup（新 turn）；一步的 turn 无 step 边界，
  插话自然降级 followup，无需特判。turn_end 从"只写 log"升级为总线真事件
  （stage02 的 UI/测试要等它）。
- 测试基建：FakeLLM 加 first_call_gate（把"消息在 step 在飞时到达"做成确定性
  时序）和 first_call_tool（把 turn 撑成两步，steering 才有 step 边界可用）；
  Harness 的 turn_end 用 Queue 缓存不用 Event——多 worker 并发收尾时 Event 的
  set/clear 会互相吃掉信号（真踩到）。
- 真实 LLM 实测（qwen3.7-flash）：三连问 demo 完美命中——插话 9.97s 发出，
  18.5s step 边界被 drain，模型自发 search("会议室 预订") 并同轮答两问；
  log 里 steering 行 ts=9.97 但按消费顺序排在 18.5 之后，"到达时刻≠消费时刻"
  是正文的教学点。API 慢（一步 8.5s）反而成了"steering 等待真实发生"的证据。
- 踩坑：turn_end 起初只写 log 没 publish，demo 卡死等不到——事件要让订阅者
  等到就必须上总线。BSD grep 在 zsh 双引号里 \| 不生效，用 grep -E。

## stage02 demo 可视化改造（rio 反馈：demo 看不出 steering 效果，要构造多步 tool_result 链）
- rio 说法核实：多步构造其实已有（第二问必调 search），真缺陷是**屏幕上看不出**
  steering 发生（只能翻 log）+ sleep(1.0) 竞态可能静默降级 followup。两点都成立，已修。
- agent 新增两个事件：steering_consumed（drain 实际消费时发，payload texts）、
  tool_call_started（_step 里首个 tool_call_delta 到达时发）。
- demo 重写：插话时机 = 等 tool_call_started（step 确定在飞）与 turn_done 竞速，
  FIRST_COMPLETED；结局两种都上屏——★ steering 生效 / "错过 drain 窗口 → 降级
  followup"。关键教训：**插话降级 followup 后 demo 不能在第一个 turn_end 就
  stop()**，否则收件箱里的消息连 worker 一起被掐死（真实发生过：插话晚 10ms
  错过 drain，turn 收尾没带上，stop 杀掉未消费消息）。修复后真实运行 ★ 命中：
  10.68s 插话 → 10.92s drain → 模型自发 search("VPN 申请") → 同轮答报销+VPN，
  log 里 steering 行 ts=10.68 排在 10.92 tool_call 之后（到达≠消费）。
- 场景换血：玻璃杯→报销/VPN。原因：第一轮 search("保温杯 库存") 按词匹配把
  玻璃杯行也带回（都含"库存"），第二问模型从上下文直接答、不调工具，demo 等
  tool_started 会卡死。报销答案绝不在第一轮检索结果里，必然调工具。
- 新增测试 test_message_arriving_after_last_drain_degrades_to_followup
  （一步 turn + gate，插话错过 drain → 两条 turn start、无 steering 行），
  stage02 共 5 passed，stage01+02 合计 8 passed。
- 第二问：__main__.py 是 `python -m <pkg>` 的模块入口约定，已答。
- 坑复发+新解法：Edit 报成功但内容未落盘又发生 3 次（agent.py 的
  tool_call_started、__main__ 的 steering_seen=False、早前的报销段）。
  对策升级：每次关键 Edit 后必须 grep 计数验证；__main__.py 曾出现
  docstring 新/正文旧的混合体，靠 grep -n 全量定位后重新编辑。

## main.py 改名 + stage03 完成（rio 指令：都改成 main.py；开 stage03，语言别戏剧化）
- 改名：stage01/02 的 __main__.py → main.py（git mv），pyproject scripts 与
  .venv/bin 两个 wrapper 的引用同步改，回归 8 passed。正文此前没提 python -m，
  无需改。坑：BSD sed 多表达式/-i 语法在这环境连续翻车，改用 perl -pi / Edit 工具。
- stage03（src/baby_event_driven_agent/stages/stage03_interrupt/，4 tests passed，
  全系列 12 passed）+ books/event-driven-agent/03-interrupt.md + pyproject/
  wrapper 的 stage03-demo/stage03-test。
- 设计（比大纲原稿的 ctrl 双队列方案简单）：中断不是消息不进收件箱；每 session
  记 _inflight step task（流消费+工具执行整体），on_interrupt 无 await 查表 cancel
  （原子、无残留）；工具结果由 turn 协程在 task 成功后统一 append，history 永远
  合法、被掐 step 不留半截；except CancelledError 里区分"step 被中断取消"vs
  "turn 被 stop() 取消"（后者 re-raise）；task.done() 后信号落空=取消与完成竞速，
  如实打印。大纲 Stage 3 已改为与实现对齐（去掉未实现的 ctrl 队列/两段式取消）。
- 测试期望 vs 真实语义的修正：排队消息在 turn 被中断后，会被下一 turn 开始前的
  drain 当 steering 消化（消费那一刻分类），不是各自开新 turn——按真实语义改测试。
- 真实 LLM 实测（qwen3.7-flash）9.00s 停止命中、9.02s step_cancelled+turn_end、
  9.02s 新 turn 开跑（恢复 <20ms）；亮点行为：中断不作废 history，被中断的
  "报销"问题留在上下文，模型下轮主动 search("报销 规定")+search("VPN 申请")
  并发补答——写进了正文，并注明"不想保留就在中断时撤 user 消息"的另一个选项。
- 正文风格按 rio 要求收敛：无 ★/■ 符号、无戏剧腔，句子平实；md 遵守每句一行。
