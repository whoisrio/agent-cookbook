# 2026-07-21

## 修复 README 章节链接失效
- 现象：README 章节链接指向 `chapters/01-*.md`，实际文件在 `chapters/langgraph/` 下，链接打不开。
- 处理：将 01~07 链接改为 `chapters/langgraph/*.md`；第 08 章文件不存在，链接后标注"待补"。
- 判断依据：保留 `langgraph/`、`deepagent/` 系列子目录分类，选择改 README 而非挪文件。

## 核实配图 assets 引用问题
- 用户提醒遗漏的 `assets/` 目录（仅 `promptchain.png`）。02 章引用该图，存在文件名大小写错误（`prompChain.png` vs `promptchain.png`）与相对路径少一层（`../assets/` 应为 `../../assets/`）。
- 03、06 章另有 mintcdn.com 外链图，发布前建议本地化到 assets/。

## 发现配套 demo 代码项目（修正前两轮判断）
- 用户指出配套 demo 在 `/Users/rio/repos/myprjs/demos/py-langgraph-demo/`（独立仓库）。覆盖 02~06 章（basicGraph/pregel/llm + langgraph.json 可部署）。
- 此前判断"examples 空、src 是孤儿，下一步补代码"方向错误：代码已存在，问题是教程未对接。agent-cookbook 的 `src/agent_cookbook` 是 demo 的镜像副本。
- README 误导读者去空 examples/；章节正文完全未提及 demo。

## 把 demo 转成 notebook 写入 examples/
- 用户确定方向：参考 demos/py-langgraph-demo 的 .py，转成 Jupyter notebook 放进 examples/，与章节对应；pyproject.toml 已含 jupyter/ipykernel 依赖（已核实）。
- 由子代理生成：examples/ 下 6 个 .ipynb（01~06 章映射）+ `_common/llm_utils.py`（合并 app/llm 的 llm.py+streamHelper.py 作共享依赖）+ gen.py（重建脚本）+ langgraph.json 副本（06 演示）。
- 验证：6 个 notebook JSON 合法、无残留 `from app.llm`；02 质量好。import 改写正确（`sys.path` 引 `_common/llm_utils`）。
- 已知坑：02/03/04 需 .env+API Key 才能跑；05 的 graphwithtopic.py 原 demo 源码本身不完整（缺 compile/invoke），已按原样标注；examples/langgraph.json 为副本冗余。
- 修复 `.gitignore` 忽略 *.ipynb：加例外 `!examples/**/*.ipynb`，配套 notebook 可提交。
- 遗留：src/agent_cookbook 与 _common/llm_utils 重复，待用户决定去留；README 仍写"示例代码见 examples/"但现已有内容，可补一句说明 notebook 对应章节。

## VSCode 打开 examples/ notebook 的 jupyter 环境
- 关键纠正：notebook 应使用的 jupyter 环境是 **agent-cookbook 自己的 .venv**（`/Users/rio/repos/myprjs/agent-cookbook/.venv`），不是 demos/py-langgraph-demo/.venv。理由：agent-cookbook/.venv 已含 jupyter+ipykernel+langgraph+langchain-openai+openai+pygraphviz+dotenv（pyproject 声明依赖，uv sync 已装）；demo/.venv 缺 ipykernel，无法作 notebook kernel。
- 已用 `ipykernel install --user --name agent-cookbook --display-name "Python (agent-cookbook)"` 注册具名 kernel（位置 /Users/rio/Library/Jupyter/kernels/agent-cookbook）。
- 已把 6 个 notebook 的 metadata.kernelspec 写死为 name=agent-cookbook，打开即自动选用，无需手动选。
- 提醒读者：VSCode 默认 notebook cwd = 文件目录（examples/），故 notebook 内 `sys.path.insert(0, '../_common')` 与 `load_dotenv()` 找 examples/.env 均正常；勿把设置 "Jupyter: Notebook File Root" 改成 `${workspaceFolder}`，否则路径错位。02/03/04 需 examples/.env 填 API Key。

## 修正：notebook 公共依赖改用 src.agent_cookbook + VSCode 自动识别收口
- 用户指出两点：①VSCode 并未自动识别 .venv 的 jupyter 环境（上次"打开即自动选用"夸了）；②notebook 应引用 src 的 llm。核实：notebook 实际引用的是 `examples/_common/llm_utils.py`（我新写的拷贝），并非 `src/agent_cookbook`；二者内容重复。
- 真问题：`dotenv_values('.env')` 读的是**进程 cwd** 下的 .env，且 02/03/04 首格会因缺 .env 直接 KeyError；运行目录和 .env 位置双卡死，很脆。已实测两种情况均 KeyError。
- 收口动作：
  1. 删除 `examples/_common/llm_utils.py`（去掉重复）。
  2. notebook 公共头改为 `sys.path.insert(0, os.path.abspath('..'))` + `from src.agent_cookbook import ...`（单一真相源）；04 用 `openai_client as openaiClient` 别名兼容 demo 源码里的 camelCase。统一经 `examples/gen.py` 重新生成 6 个 notebook，kernelspec 写死 `agent-cookbook`。
  3. `src/agent_cookbook/llm.py` 的 `.env` 改为按仓库根相对路径读取（`os.path.join(_REPO_ROOT, ".env")`，_REPO_ROOT 由 __file__ 推算），不再依赖 cwd。`.env` 位置统一为**仓库根**（与 .env.example 同位置）。
  4. 新建 `.vscode/settings.json` 设 `python.defaultInterpreterPath` = `${workspaceFolder}/.venv/bin/python`，让 VSCode 自动锁定 .venv（解决"不自动识别"）。
  5. README 补「配套 Notebook 章节对照表」+ 快速开始（uv sync / cp .env.example .env / VSCode 内核自动选 "Python (agent-cookbook)"）；01/05/06 不需 Key，02/03/04 需。
- 验证：临时假 .env 于仓库根，分别在 cwd=examples 与 cwd=/tmp 下 `from src.agent_cookbook import ...` 均成功，证明 .env 不再依赖运行目录。测试后已删除假 .env。
- 注意：gen.py 里 demo worker 文件名是 `orchestatorWorker.py`（原仓库拼写少一个 r），重写时曾误写成双 r 导致生成失败，已改回。

## Review & 修复 chapters/langgraph/04-langgraph-stream.md
- 用户要求严格 review 该文档。对照官方 docs 逐句核实。
- 致命错误：`workflow.event_stream()` API 名称不存在，应为 `graph.stream_events()`。已修复。
- 概念修正：
  - Stream vs EventStream 对比中，v2 格式的 Stream 输出描述错误（写成了 v1 的 tuple 格式），已修正为 StreamPart dict。
  - "updates/checkpoints/tasks/debug 没有等价投影"过于绝对——EventStream 可通过自定义 transformer 消费，只是没有开箱即用的投影。已修正。
  - 投影表格漏了 `stream.extensions`，已补。
  - messages 模式描述过于模糊，已改为官方表述 "(token, metadata) 二元组"。
  - Stream/EventStream 对比代码 async/sync 不一致，已统一。
- 轻微问题：全文 event_stream 术语不统一，已全部改为 `stream_events` 或 Event Stream。
- 冗余修复：Event Stream 介绍段落有两句重复，已合并；优势列表中"不用操心复杂 tuple"与已修正的 v2 格式矛盾，已改写。

## Jupyter kernel 简化 & gen.py 清理
- 用户明确：只操作项目目录内环境，不要碰用户级东西。
- 删除了之前自己擅自创建的 `~/Library/Jupyter/kernels/agent-cookbook/`（用户级 kernel）。
- 同时删了 `.venv/share/jupyter/kernels/python3/`（argv 用裸 `python`，不可靠）。
- 现仅保留 `.venv/share/jupyter/kernels/agent-cookbook/` 一处 kernel，argv 为绝对路径。
- `.vscode/settings.json` 增加 `jupyter.notebookFileRoot` 和 `notebook.kernelPicker.type` 配置。
- 用户确认 gen.py 无用（后续示例不再写 py-langgraph-demo），已删除。
