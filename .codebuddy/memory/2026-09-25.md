# 2026-09-25

- stage03/04 demo 重录 + 书稿 gif 对位（2026-09-25）：磁盘上的 rec gif 全是旧版 demo 录的（stage03 还是"等工具跑完"旧行为且含已删除的 00-baseline；stage04 是被废弃的 6 段结构 render-per-event/flood/veto/inbox-priority），用 recorder --run-id docs --render 全部重录并渲染。**stage03 行为已改**：场景 4（07/08）tool 执行中 = 当场掐死（step_cancelled，tool_call 随 step 丢、一条 tool_result 都不进 log/history），不再是"等跑完+turn_interrupted 边界命中"；边界命中标本现在是 09。03 章 demo 段整节对齐（删 00-baseline 段、case 数 11→10、落点名 toolcall/tools-done→tool_start/tool_running/tool_done、07/08/09 重写+全部 log 片段换最新一轮实测）。04 章 5 个 case 各嵌入同名 gif。**修了 demo 脚手架 bug**：stage03 main.py 的 gated_search 只应拦第一次调用（原来 redirect 转向轮再调 search_rules 会二次挂进死门，turn 永远等不到）。test_stage03_live.py 有 2 条过时用例（test_tool_phase_stop_waits_then_closes_with_assistant、test_boundary_redirect_is_plain_steering）断言旧语义，本来就 fail，待用户改写。
- 遗留：rec/stage03/docs/00-baseline.* 与 rec/stage04/docs/ 的旧 6 段产物（01-render-per-event-vs-buffered / 01-sync-vs-async / 02-burst-load / 02-flood-all-delivered / 03-slow-subscriber / 04-permission-veto / 06-inbox-priority / all.*）删除命令两次未获批准，文件还在，需要用户手动删（文档已无任何引用）。
