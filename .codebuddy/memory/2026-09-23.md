# 2026-09-23

- 05a-trajectory.md"带摘要 rewind"一节整节重写：原来把"rewind 到已有 compaction 节点"和"branch_with_summary"混在一起，是用户困惑的根源。改用具体例子 e1 e2 | e3 e4 e5 compA(keep_from=e3) e6 e7 e8：rewind 到 compA = 纯移指针，视图回到压缩刚做完那一刻 [system, <摘要>, e3, e4, e5]；rewind 到 e5 或更早 = compA 不在路径，压缩没发生过。branch_with_summary 是另一回事——新建 branch_summary 节点，按树上位置出现在视图里（compaction 摘要插最前，branch_summary 顺位 append），语义已对照 trajectory.py 的 branch/branch_with_summary 和 agent.py 的 build_context 确认。
- 追加：两个 rewind 落点各补一幅 rewind 后的树形图（含"继续对话后新 entry 与谁同父"）和投影结果，收尾一句点明"文件不动，差别只在路径与投影"。
- branch_with_summary 小节开头补场景：裸 rewind 后新分支不知道旧分支踩过的坑，可能重走弯路；该 API 在 rewind 同时把调用方写的结论作为 branch_summary 节点带进新分支。
- 用户确认理解："branch_with_summary = summary and rewind"。书里补了两步实现（先 branch 移指针、再 append 摘要节点认 keep_from 为父）及顺序约束（append 认"此刻的 leaf"，反序会挂错地方）。
- 用户质疑 branch_with_summary 是不是自造能力（"不是 rewind 的基操，我没要求这个能力，哪个 agent 有？"）。查证：pi 有——pi.dev 文档 Sessions/Session-Format：/tree 切分支时把被弃分支总结成摘要附加到新分支，entry 类型 branch_summary（fromId+模型生成的 summary+usage），SessionManager.branchWithSummary。已改书：正文标注出处（pi 的能力，非 rewind 基操，摘要文本归宿主），对照表修正（原表误写"本章另有 branch_with_summary"）。
- 用户问 pi 是否默认自动生成：不是，是提议制——仅 /tree 切分支时提议，用户确认后才由模型生成（公共祖先→回溯收集→token 预算→LLM 总结→追加 BranchSummaryEntry）；无独立开关；扩展钩子 session_before_tree 可取消导航或代供摘要。书里对应行已补"提议、用户确认后生成"。
- 05a 结构调整（用户要求）：删掉独立小节"机制五：会话身份与生命周期"（属基本设计），并入代码改动章节；该章节扩为"代码改动：session 层新增，agent 侧只动三处"，分 SessionStore（start/resume/close 代码 + 三条规矩）和 agent 侧三处（attach/_traj/build_context 代码）两小节，代码均摘自 store.py / agent.py 实际实现。
- 补上 Trajectory 小节（用户指出漏了核心类）：放 SessionStore 之前，讲 TrajectoryLog（header 首行 + 长度前缀/CRC 行格式）与 Trajectory（header/_by_id/leaf_id 三样状态；append 认父→落盘→索引+移 leaf；_index 守门 id 冲突/父不存在；path() 遍历；branch_* 是组合，entries()/branch_points() 观测）。
- 用户批评"守门规矩""不猜""就三样""只有一条线索"是 AI 腔，已改 5 处平实表述，并把这类修辞化计数/排比说法加进 MEMORY.md 禁用清单。
- 修 demo 报错：main.py:385 残留旧签名 build_context(traj, system_prompt=...)——9-22 投影重构（98362db）把签名改成只收 traj（prompt 从 header/prompt_change 提取）时漏改了 demo recovery 段。删掉参数即修复（Harness 的 store.start 已把 prompt 写进 header，输出不变）。demo 六段全跑通，离线测试 36 passed。
- 04-message-mechanism.md 开头段：用户自己重写（三个场景：等待可定义/优先级/洪峰，对应改动一/二/三），要求 review 而非代写——第一版我把他举的场景抽象化被用户驳回（"不就改回去了"），正确做法是最小修正只动硬伤：结尾"下行"→"上行"（方向反了）、打断信号删"上行的"（它走下行旁路）、中英文空格/UI 大写。教训：用户自己写的场景例子必须原样保留，review 只指出错误和排版。
- 04 开头新段落与"需求：上行和下行"一节重复，删掉该节前三章过渡段（保留对比表 + publish + 六件事清单）；改动一 emit 代码后补一段划清"等治理落盘 / 不等订阅者"的边界（治理只有 INTERCEPT 参与、token 流跳过、50ms 预算封顶），细节留改动五。
- 对照调研：pi（badlogic/pi-mono）tool_call 拦截 = 串行 await middleware 链（block/改 input/抛错默认阻断），与 emit 同步治理同路；但 pi 允许在拦截 handler 里直接 await ctx.ui.confirm 等人（无预算超时），书里明确反对（ASK + agent 另起等待）。pi 无 QoS 分道/背压（串行链 + drain 屏障）；hermes-agent（Nous Research）同步 loop + 进程内回调，无队列，审批是 approval/sudo 同步回调，持久化是 SQLite 会话库非事件流。两家都没做位点/回放。
- 用户质疑 04 的 seq/correlation_id 在 05a 没用上、"seq 被 parentId 取代"。查证：05a 两本账——EventLog（seq，传输层，store.py:98 恢复时 read_since 闭合悬空审批）+ Trajectory（entry id，会话层）；两本账零互链（Entry 无 seq 字段）。seq 在 5a 无真实消费者（read_since(0) 全量读、按 request_id 配对；offsets 零调用），stage04 内 seq 消费者全是自证（demo06/测试/审计叙事）。
- 第四章重构方案定调（用户确认方向，未动手）：04 = 纯传输行为（下行优先级小节、上行异步分发+队列满策略、合并缓冲、治理、审批），**落盘整块挪 5a**；Event 回四件套（seq/corr 删）；emit 三步（治理→入队）；demo 六段变五段。5a 要补：EventLog 正式一节（定位+审批闭合+审计链）、emit 落盘步回归（含"被否决照样落盘"规矩搬家）、两本账小节扩正节、恢复节补 EventLog 残尾对称。执行时注意：整节重读重写，正文写给读者不回应作者质疑。
- **第四章重构执行完毕**（用户下令"先把第四章的文档和代码处理了"）：
  - 代码：stage04 删 persistence.py、Event 回四件套（删 seq/correlation_id/envelope）、bus emit 三步（删 _persist/_next_seq/record/log 参数/last_seq）、agent 删 _corr 与 record 留痕（迟到答复不认领+logger）、subscribers/agent docstring 对账；
  - 测试：transport 删 6 条（seq×3/torn/offset/redact/retention）新增 deny 不投递 1 条，approval 的 decided() 改订阅者捕获、stale 用例改"不认领"，live 删信封断言、滞后断言分母改 counter.total（曾因分母不一致挂过一次）、合成消息改 seen_events 断言；**18 passed**（16 离线+2 真模型，ollama）；
  - demo：六段→五段（06 删），demo05 去掉 seq 打印与"手抖留痕"段，demo04 注释指向下一章事件账；
  - 录像：cookbook-rec --stage 04 --run-id docs --render 重录全部 5 case + all，旧 06 产物已删；
  - 书稿：04 章整章重写——删改动四（信封）/改动六（落盘），改动五→改动四（治理），规矩 6 条→5 条（record 留痕归 5a），六件事→四件事，代码小结/跑一下/验证（18 passed）/降级项（加"事件账归 5a"首条、删 fsync/脱敏/位点并发/record 后门条、多实例条去 seq 语义）/下一章（两本账+落盘工程归 5a）全部对齐；实录引用换新 docs run 数字。
  - 遗留：②IDE 对已删 persistence.py 的 Pylance 报错是缓存，刷新即消；③05a 书稿待补 EventLog 节（见上条清单）。
- **第四章收尾完成（下行优先级 + redirect）**：Event 加 `priority: int = 100`（小者先，DEFAULT_PRIORITY 与 Subscription.order 同惯例；排队元数据非事实字段）；收件箱 asyncio.Queue→PriorityQueue，堆元素 (priority, 到达序号, event) 保同优先级 FIFO；enqueue 旁路不变（stop/redirect/approval）。测试 test_stage04_inbox_priority.py 4 条：弹出序+drain 序（注意语义：队列消息在 step 边界 drain 全算 steering——消费时机定类别，测试期望曾按"多轮 followup"写而挂，改为"首弹+drain 序"两断言）、steering drain 优先级序、stop 旁路、redirect 纠正先于排队消息。demo 新增 06-inbox-priority（独立、不打模型、脚本化 LLM+慢工具探针；banner 需 combined names 表；only-06 早退分支；"all"含 06）。书稿：改动五（下行优先级）新节（三条规矩：优先级不参与分类/旁路高于队列/不做饥饿老化）、次序总结"旁路 > 队列(priority) > 到达先后"、跑一下/验证/降级项（inbox 条补饥饿老化）/小结表对齐。**22 passed**（20 离线+2 真模型），case 06 已录 docs run。待办：05a 补 EventLog 节。
- 用户追问"token 流这种 eventlog 记录来做什么"——结论：不该记。现状 stage05 _persist 无差别落 delta（demo 86 条里 30 条是 delta），逐个盘点消费者（审批闭合/滞后验证/统计）均不需要。5a 待办修正：EventLog 记治理裁决+审批请求回执+生命周期锚点，token 流投递即焚不落盘（emit 落盘步按 stream 道过滤）；两本账分工更干净：delta 两本账都不进（会话账记合并结果 message，传输账记可审计事实）。
- 用户发现重写稿仍保留"改动二：QoS 分道：洪峰淹没不了信号"独立节与需求清单"洪峰淹没信号"条目——与定稿的"分道降级为队列满策略"方案不一致，已修正：该节降为改动一小节"### 队列满了怎么办：可丢的丢，不可丢的背压"，需求清单改为上行三件事（解耦/满了策略+合并缓冲/治理审批）+下行一件事（优先队列），改动重编号（一二三四），交叉引用与跑一下/验证/小结的"QoS 分道"措辞全部清理。教训：整章重写后要按定稿的结构决议逐节核对，不能凭记忆保留旧框架。
- 开头段按用户要求改为"从解决上行开始讲"：三个场景保留但重排——等待可定义/呈现快慢/治理留在上行部分，优先级（打断信号插队）挪到收尾句"最后回头补一刀下行"。
- **stage04 最终收尾（轨迹唯一事实层口径）**：用户裁定 EventLog 不需要存在、Trajectory 是唯一事实基础（ approval_required 写成轨迹 entry、resume 扫轨迹闭合悬空审批、裁决元数据进占位 entry payload、迟到答复不留档）。04 章与 stage04 代码的全部"事件账/事件流/留成账"口径已改为"轨迹是唯一事实层"（events/bus/agent docstring、main.py demo04 注释、书稿需求尾/治理规矩1/审批规矩3/单进程边界/降级项首条/下一章），改动三实测引文按新录 case 04 更新（1 条占位、新说明文案），case 04 已重录 docs run。**22 passed**。下一章交接语已写明：审批请求/回执是独立 entry、裁决元数据进占位 payload、恢复扫轨迹闭合悬空审批。05a 重构（删 transport/persistence、seq/corr、审批 entry 化、书稿"事实层归一"节）待用户下令执行。
- 用户质疑洪峰设计（QoS 分道/背压）对本地单 agent 是过度设计 + 生产多 agent 场景进程内 EventBus 是否合适。结论：本地单订阅者确实可砍到只剩改动三（消费者侧合并缓冲）；生产化不是放大 EventBus 而是复制+外置——每进程留瘦总线管热路径，生命周期事件发 broker，高频 delta 不灌 Kafka。用户进一步指出降级项"改的是 Lane 的落地方式"说过轻——已改写该条：多实例化跟着动的是 seq 语义（全局单调→per-session 分区）、落盘位置（"落盘只有一处 emit 里"不成立）、投递语义（一次必达→at-least-once 消费者幂等）、单写者前提（session 亲和钉分区）；真正留下的只有信封字段和按可丢性分道的决策（对应不同持久化等级的 topic）。
