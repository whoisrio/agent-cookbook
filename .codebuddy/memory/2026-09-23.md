# 2026-09-23

- 05a-trajectory.md"带摘要 rewind"一节整节重写：原来把"rewind 到已有 compaction 节点"和"branch_with_summary"混在一起，是用户困惑的根源。改用具体例子 e1 e2 | e3 e4 e5 compA(keep_from=e3) e6 e7 e8：rewind 到 compA = 纯移指针，视图回到压缩刚做完那一刻 [system, <摘要>, e3, e4, e5]；rewind 到 e5 或更早 = compA 不在路径，压缩没发生过。branch_with_summary 是另一回事——新建 branch_summary 节点，按树上位置出现在视图里（compaction 摘要插最前，branch_summary 顺位 append），语义已对照 trajectory.py 的 branch/branch_with_summary 和 agent.py 的 build_context 确认。
- 追加：两个 rewind 落点各补一幅 rewind 后的树形图（含"继续对话后新 entry 与谁同父"）和投影结果，收尾一句点明"文件不动，差别只在路径与投影"。
- branch_with_summary 小节开头补场景：裸 rewind 后新分支不知道旧分支踩过的坑，可能重走弯路；该 API 在 rewind 同时把调用方写的结论作为 branch_summary 节点带进新分支。
- 用户确认理解："branch_with_summary = summary and rewind"。书里补了两步实现（先 branch 移指针、再 append 摘要节点认 keep_from 为父）及顺序约束（append 认"此刻的 leaf"，反序会挂错地方）。
- 用户质疑 branch_with_summary 是不是自造能力（"不是 rewind 的基操，我没要求这个能力，哪个 agent 有？"）。查证：pi 有——pi.dev 文档 Sessions/Session-Format：/tree 切分支时把被弃分支总结成摘要附加到新分支，entry 类型 branch_summary（fromId+模型生成的 summary+usage），SessionManager.branchWithSummary。已改书：正文标注出处（pi 的能力，非 rewind 基操，摘要文本归宿主），对照表修正（原表误写"本章另有 branch_with_summary"）。
- 用户问 pi 是否默认自动生成：不是，是提议制——仅 /tree 切分支时提议，用户确认后才由模型生成（公共祖先→回溯收集→token 预算→LLM 总结→追加 BranchSummaryEntry）；无独立开关；扩展钩子 session_before_tree 可取消导航或代供摘要。书里对应行已补"提议、用户确认后生成"。
