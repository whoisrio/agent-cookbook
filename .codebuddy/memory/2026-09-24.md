# 2026-09-24

- subscribe 校验四轮重构终局（2026-09-24）：①bus.subscribe 硬编码 MAILBOX_ONLY→StreamConsumer → ②Subscription.__post_init__ 自检（用户驳：检查还在订阅者里）→ ③总线进门调 require_consumable（用户驳：又耦合回总线）→ ④**子类型方案**：`Event` 纯四件套 + `HANDLER_SHAPE: ClassVar[type] = object`；`StreamEvent(Event)` 声明 `HANDLER_SHAPE = Mailbox`（上行"谁消费我"）；`UserMessage(Event)` 声明 `intent`/`priority`（下行路由元数据，基类不掺）+ `EVENT_CLASSES` wire 名注册表；总线 subscribe 只调 `events.validate_subscription(sub)`——一条对所有类型相同的通用规则，加新约束类型零改总线。enqueue 对非 UserMessage 的排队消息抛 TypeError。测试钉"构造不校验、进门才校验"。27 passed（25 离线+2 真模型）。设计口径：**知识归事件子类（声明），执行归绑定形成处（一条通用规则），订阅者纯数据**；用户对"校验必须有人执行"接受通用规则+子类声明，不接受任何 per-type 分支散落在总线/订阅者。书稿被用户重排（优先级章节提前、六段 demo 演进出 promote/双队列），只做最小对齐：测试数 25+2、优先级一节补 UserMessage 一段。
- 治理离开总线（用户驳"Governor 挂总线上=换地方做 check，无本质区别"）：终局 = governance.py 的 `Rule`/`Governor.check(name, arguments)→Verdict` 是 **agent 工具执行路径上的准入关卡**，与 `Tool.requires_approval` 并排在 `_execute_call`；`before_tool_call` wire 事件删除（规则收 (name,arguments) 不收事件）；`EmitResult`/`INTERCEPT`/`OBSERVE`/Subscription.mode 删除，Subscription 收窄为纯观测声明，bus.emit 无返回值纯分派；tools.py 抽离（Tool: schema/fn/requires_approval/approval_reason），llm.py 纯客户端。
- 04 书稿对齐完成（2026-09-24）：emit 引文改纯分派、治理节重写（Rule/Governor + agent 执行路径两道关卡 + 审批回路说明）、治理点注记/跑一下/验证清单/测试数（24 离线+2 真模型）全对齐；grep 确认无 EmitResult/before_tool_call/INTERCEPT/approval_policy 残留。**未动**：emit 节后面的 Lane 引文（77-90）与"道满"叙述（~160-165）——代码已是直接分派+消费者侧合并缓冲，书稿那段是用户在途重写，留给用户。
- 重跑收尾（用户下令）：全量 stage04-test **26 passed**（15.7s，含 2 条真模型 live）；demo 04/05 重录完成（cookbook-rec --run-id docs --render），新录像 banner（"治理规则"/"Tool.requires_approval"）与实录行已核对，与书稿引文一致（"被治理拦下"/"rules.txt 未被改动"/"确认结果：allow by user" 均未变，引文无需再改）。第四章本轮重构（工具声明审批 + 治理离开总线 + 事件子类型）全部闭环。
- 已提交并推送：commit `ba65838`（44 files, +2686/-3374）推送至 origin/main。

- **stage04 下行意图模型落地**（用户裁定：控制权交还发布端，stage02 章不动、stage04 书稿写明演进取代）：Event 加 `intent: str = FOLLOWUP`（followup 排队等下一轮 / steering 下个 step 前插话）；收件箱拆 `_followups` + `_steerings` 两条优先队列（堆元素 (priority, 到达序号, event)，同级 FIFO）；worker 空闲先取 followup、空了取 steering（降级成主输入，实现用 FIRST_COMPLETED 竞速 + 落败方取消、双就绪时 steering 放回）；`_drain_steering` 只消费 steering 队列（followup 用户说了等，不碰）；stop 后 worker 立刻按优先级接上 followup。测试 test_stage04_inbox_priority.py 重写 6 条（followup 不插话 / steering 优先级 drain / 两类不混 / 降级 / interrupt 接续 / redirect 先于插话），**24 passed**（22 离线 + 2 真模型）。demo 06 重写为三类消息各归各位并重录 docs run。
- 用户指出 promote 竞态：turn 刚结束的取件窗口里 followup→steering 的移动会乱序，且 PriorityQueue（堆）无法精确移动"指定的那一条"。修正：收件箱从 PriorityQueue 改为**按序列表**（append 到达，消费取 (priority, 到达序) 最小值），新增 `promote(event) -> bool`（remove+append 同步无 await，与 worker 取件天然互斥；已被取走返回 False）；`_next_message` 用唤醒事件 + 双检查防丢唤醒；`_drain_steering` 用 sorted(inbox) 整批消费。测试 +2（promote 只动那一条/已被取走返回 False），**26 passed**（24 离线+2 真模型）。demo 06 加 promote 环节并重录；书稿改动四补规矩 4（promote）与队列实现说明（列表取最小值而非堆弹出）。
- 用户追问为何需要 wake：收件箱换成列表后没有阻塞 get，wake 是 enqueue 叫醒 worker 的最小原语；clear+双检查是防丢唤醒的经典组合（交织：worker 查空 → enqueue append+set → worker clear 擦掉信号 → 不 re-check 就永睡）。promote 里的 wake.set() 是多余的（worker 挂在 wait 上时两队列必空、promote 必 False）已删——不变量收敛为"只有 enqueue 唤醒"。
- 用户指出"promote 后 turn 刚好收尾"的顺序反转：M 先到、promote 后最后一个 drain 已过，逐队列取会让后到的 F2 当主输入、M 以插话身份排后面。修正 `_pop_next` 为**两队列合并取 (priority, 到达序) 最小**（跳队只发生在活 turn 内的 drain；错边界回归全局到达序）。测试用 ScriptedLLM 的 {"sleep": s} 伪指令在最后一段流中间挂起，确定性构造"错过边界"窗口；test_promote_missed_boundary_keeps_global_order 钉死到达序。**27 passed**（25 离线+2 真模型）；书稿改动四取件规则与 promote 规矩 4 已同步。
- 类序测试 test_steering_class_beats_followup_priority：M（steering p100）先于 F2（followup p1）——priority 只在类内排序不跨类竞争。调试中发现两个测试构造错误并修正：①同文本消息发了两条（send + 手工 Event）；②wait_turns 不足 3 轮时 h.stop() 打断在飞 turn，stop 的合成标记（STOP_MARKER 经 user_input 事件）污染 inputs 断言。**28 passed**（26 离线+2 真模型），case 06 重录，书稿计数（26 离线+2）与验证节同步。第 4 章全部完成。
- 9-23 遗留至此全部清完。下一站：05a 事实层归一（删 transport/persistence、seq/corr、审批请求/回执 entry 化、resume 扫轨迹闭合悬空审批、书稿"事实层归一"节、demo 重录）。
