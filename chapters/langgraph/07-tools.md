
2022年底，chapgpt3.5横空出世的时候，LLM只能提供聊天服务，这个时候，虽然有AGENT的概念，但是他只是一个没有手的家伙，AGENT = Model+Prompt；
后来，openai在/v1/chat/completions/添加了function call的能力，让模型能够借助工具调用外部的服务，于是才有了构建agent的基础
AGENT = Model + Context + Tools；
再后来，模型能力越来越强，但是依然不能100%解决不了LLM幻觉、注意力漂移等问题，出现了Harness，AGENT = Model + Harness(Context + Tool)；
再后来，持续改进， AGENT = Model + Harness(Context + Tool) + Eval；

LLM本身是没有记忆的，
Context 关注的是如何正确的时机，给模型提供正确的上下文，让模型实现正确的推理；
Tools 解决的是如何让模型能够真正的为人类工作，没有Tools，模型就是关在笼子的大脑。

模型理解Tools，也是通过agent在prompt中提供的描述来理解的

生产级的Tool ? 
- 什么场景适合用工具
- 如何定义好的工具
- 如何确保工具的安全性，同步审核，边车审核，规则引擎
- 如果避免工具撑爆上下文，tool_search
- 异步工具执行，工具的异步设计，占位符等等

## Tools定义，让模型更好的理解工具
- description，
- 参数描述，
- 使用样例，fewshot



```python
@tool
def custom_tools(para1: str)->str
  """
  当xxx时使用该工具

  para1:
  """
  #...
  return ''
```

tools超多的时候，工具的分类分层定义



## 工具调用，
- 参数格式验证
- 权限验证
- 明确的错误提示

## 错误围栏，
- 外部错误，api级重试，降级；控制重试等待时间 ? 
- 工具调用了llm，备份切换，比如rag本身是个tool
- 工具执行整体重试 ? 幂等；难以实现幂等的工具，如发邮件，打电话，窗口限制
- llm + tool调用死循环 ? 1.max turn； 2. 相同参数调用的次数限制；
- 

## mcp，防注入


## 安全防护

### 工具执行环境
- 最小权限，cgrp，资源限制 ? 
- docker
- vm

### prompt注入

- tool_description的prompt注入
- 执行结果
- 




今天主要看了一下 AI Agent Book 的 Tools 这一部分，和一部分 Agent Eval 的内容。
Tools 的这一部分我觉得讲得还是挺清楚的：

首先是从场景上对比了什么时候应该用 Tools，主要是对比 Skill。因为 Skill 其实在很多情况下也能去调用工具。具体来说：
1. 如果是一系列的操作流程，用 Skill 来封装会比较合适。
2. 对于一些相对来说复杂一些的工具使用，Skill 相当于提供了一个工具的说明文档，这种场景也比较适合用 Skill。

Tools 本身因为可以用 function call 的方式来定义工具的输入输出，如果用 Tools 来描述工具的话，相对来说可以得到更好的参数调用确定性，也更容易直接在代码层面去对模型调用工具填充的参数来做校验。

作者把工具分成了五类：

1. 查询类工具
  ◦ 常见场景：查文件、查天气、搜索、查询知识库等。
  ◦ 特点：天然具有可以并行执行的特点。
  ◦ 安全与权限：虽然不改变数据，但也需要有对应的权限管理，避免秘密或个人敏感信息暴露到模型的上下文中。
2. 执行类工具
  ◦ 常见场景：写文件、写 Todo 等会改变数据值的工具。
  ◦ 安全与权限：对工具执行的权限、本身的环境权限以及安全权限，都需要特别的防护手段。
3. Agent 间交互类工具
  ◦ 常见场景：Task 和 Sub-agent 之类的工具。
  ◦ 特点：主要用于主子 Agent 和 Task 之间的交互。
4. 用户交互类工具
  ◦ 常见场景：比如 Claude 上的 "ask_user_question" 工具，把与用户的交互定义成一个工具来直接提供交互。
  ◦ 发展趋势：目前业界可能也在研究如何通过生成式 UI（Generative UI）去和用户交互。
5. 环境/事件驱动类工具
  ◦ 特点：主要是为了让 Agent 可以更及时地感知到环境的变化，从而触发其工作。
  ◦ 常见场景：一些定时任务或轮询监控。在非用户输入的场景下，可能会把 Agent 对接到一个消息总线上，让其可以很方便地接收到外部触发来启动工作

# 如何算是一个好的工具？主要有以下几个方面：

6. 工具的描述（Description）：
这是直接用于模型推理的。要让 LLM 能够正确选择出最适合的工具，关键在于工具的描述。描述中要写清楚“什么时候该使用这个工具”，而不是在 description 里面大量堆砌工具的说明书。
7. 工具参数的定义：
参数定义要清晰，必须有明确的类型和范围。如果是在 Python 环境中，可以借助 Pydantic 来提供非常方便的定义和校验。
8. 工具的使用示例：
最好在工具说明中给出一些使用示例，特别是针对一些有特殊参数的工具。例如定义电话号码或信用卡号，它们可能有一些特定的规则。如果仅仅列出这些规则，由于不同模型的理解能力存在差异，可能会导致偏差。因此，最好在提供规则的同时，带上具体的样例说明。

# 工具执行的能力约束
幂等，可重试；
防重复执行 ? 
哪些可以入缓存 ? 
如过没有唯一id，比如打电话，转账? 这种场景，通过记录任务状态，防抖 ? 

# 工具执行环境

- 最小的权限隔离，靠os 权限约束可访问资源
- docker，依赖文件挂载；守不住内核被攻破；
- 单独的虚机


# 生成级的工具成功执行 ? 

api 问题: 超时，断线，服务不可访问 ； 遵守api契约重试 ? 重试次数 ?  agent调用工具常见；
模型调用工具问题: 参数错误之类的 ；返回模型，让模型重试 ? 重试次数 ? 
上下文层:上下文溢出，压缩失败，轨迹结构损坏
控制流层:死循环


# 工具能力归类
dd

# 工具执行的要求
幂等，日志，正确性，

# tool_search
123 
# 安全




# 架构决策

agent 工具数量，比如超过100个工具，LLM很容易错误决策；
工具分类，引导模型先推理用哪一类工具，再从分类里挑选；
tool_search，

----
我碰到过的一些agent调用llm的错误，model不支持多模态，返回报错了，agent仍然重试； -- hermes